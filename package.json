{
  "name": "the-tab-suspender",
  "version": "1.7",
  "scripts": {
    "build": "node build.js",
    "extract-strings": "node extract-strings.js",
    "generate-locales": "node i18n-generator.js",
    "update-html": "node update-html.js",
    "i18n": "npm run extract-strings && npm run generate-locales && npm run update-html",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "esbuild": "^0.27.2",
    "fs-extra": "^11.3.3",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.44.1"
  }
}